name: Deploy

on:
    workflow_dispatch:
    push:
      tags:
         - "v*"

jobs:
  deployEpitech:
    secrets: inherit
    uses: ./.github/workflows/deployEPITECH.yaml

  deployVPS:
    uses: ./.github/workflows/deployVPS.yaml
    secrets: inherit
    with:
      tag: ${{ github.ref_name }}